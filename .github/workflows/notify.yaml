name: Notify Maintainers
on:
  # schedule:
  #   - cron: 0 12 1,15 * *

  # To test
  workflow_dispatch:

jobs:
  notify-maintainers:
    runs-on: ubuntu-latest
    steps:
      - name: Notify maintainers
        run: |
          gh issue reopen 1
          gh issue comment 1 -F message.md
